language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-grunt-plugin' ]; then cd .. && eval "mv $currentfolder generator-grunt-plugin" && cd generator-grunt-plugin; fi

